export type QueueCampaign={id:string;user_id:string};
type Db={connect():Promise<{query(sql:string,values?:unknown[]):Promise<{rows:any[]}>;release():void}>};
export async function queueCampaignRun(db:Db,args:{campaign:QueueCampaign;idempotencyKey:string;contextSnapshot:unknown;revisionNotes?:string|null;concurrencyLimit:number}){const client=await db.connect();try{await client.query("BEGIN");await client.query("SELECT pg_advisory_xact_lock(hashtext($1))",[`campaign-user:${args.campaign.user_id}`]);const same=(await client.query("SELECT * FROM campaign_runs WHERE campaign_id=$1 AND idempotency_key=$2",[args.campaign.id,args.idempotencyKey])).rows[0];if(same){await client.query("COMMIT");return{kind:"existing" as const,run:same};}const active=await client.query("SELECT r.* FROM campaign_runs r JOIN campaigns c ON c.id=r.campaign_id WHERE c.user_id=$1 AND r.status IN ('queued','running') LIMIT $2",[args.campaign.user_id,args.concurrencyLimit]);if(active.rows.length>=args.concurrencyLimit){await client.query("ROLLBACK");return{kind:"conflict" as const,activeRun:active.rows[0]};}const run=(await client.query(`INSERT INTO campaign_runs(id,campaign_id,run_number,idempotency_key,context_snapshot,customer_revision_notes) SELECT $1,$2,COALESCE(MAX(run_number),0)+1,$3,$4,$5 FROM campaign_runs WHERE campaign_id=$2 RETURNING *`,[crypto.randomUUID(),args.campaign.id,args.idempotencyKey,args.contextSnapshot,args.revisionNotes??null])).rows[0];if(!run)throw new Error("RUN_NOT_CREATED");await client.query("UPDATE campaigns SET status='queued',updated_at=NOW() WHERE id=$1",[args.campaign.id]);await client.query("COMMIT");return{kind:"created" as const,run};}catch(error){await client.query("ROLLBACK").catch(()=>{});throw error;}finally{client.release();}}
