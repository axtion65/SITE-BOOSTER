import test from "node:test";import assert from "node:assert/strict";import {readFile} from "node:fs/promises";
const source=await readFile(new URL("../routes/mockups.ts",import.meta.url),"utf8");
test("generation resolves duplicates and queues one durable job",()=>{const duplicate=source.indexOf('if(existing){');const insert=source.indexOf("INSERT INTO mockup_versions");const accepted=source.indexOf("res.status(202)",insert);assert.ok(duplicate>0);assert.ok(insert>duplicate);assert.ok(accepted>insert);assert.match(source,/FOR UPDATE/);assert.match(source,/status IN \('queued','provider_submitting','provider_processing','saving_asset'\)/);assert.match(source,/'queued','queued',NOW\(\)/);assert.doesNotMatch(source.slice(source.indexOf('router.post("/mockups/:id/generate"'),source.indexOf('router.post("/mockups/:id/versions"')),/new FalMockupImageProvider|queue\.submit/);});
test("AI Model route blocks generation without an authoritative character",()=>{assert.match(source,/hasAuthoritativeBrandModel\(project\.creation_path,project\.brand_model_id,project\.brand_model_refs\)/);assert.match(source,/Choose a Brand Model before creating this visual/);});
