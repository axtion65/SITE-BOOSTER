import test from "node:test";import assert from "node:assert/strict";import {ADULT_AGE_RANGES,brandModelInput,canCreateBrandModelMockup,DEFAULT_MODEL_PREFERENCES,findPendingCandidateSet,isPendingCandidateSet,MODEL_APPEARANCES,MODEL_PRESENTATIONS,MODEL_STYLES,requiresQuaeBrandModel,SCENES,sceneDirection} from "./brand-model-flow";
test("Let Quae choose is the default for every model preference",()=>{assert.deepEqual(DEFAULT_MODEL_PREFERENCES,{presentation:"quae_choice",appearance:"quae_choice",age:"quae_choice",style:"quae_choice",additionalDirection:""});assert.equal(requiresQuaeBrandModel("brand_model",""),true)});
test("all customer model preference options map to provider-neutral domain fields",()=>{for(const presentation of MODEL_PRESENTATIONS)assert.equal(brandModelInput({...DEFAULT_MODEL_PREFERENCES,presentation}).presentation,presentation);for(const appearance of MODEL_APPEARANCES)assert.equal(brandModelInput({...DEFAULT_MODEL_PREFERENCES,appearance}).appearanceDescription,appearance);for(const age of ADULT_AGE_RANGES)assert.equal(brandModelInput({...DEFAULT_MODEL_PREFERENCES,age}).adultAgeRange,age);for(const style of MODEL_STYLES)assert.equal(brandModelInput({...DEFAULT_MODEL_PREFERENCES,style}).archetype,style)});
test("additional direction is retained and regeneration stays explicit",()=>{const value=brandModelInput({...DEFAULT_MODEL_PREFERENCES,additionalDirection:"friendly and approachable"},true);assert.equal(value.styling,"friendly and approachable");assert.equal(value.replacePendingCandidateSet,true)});
test("scene options and custom scene remain visual direction",()=>{for(const scene of SCENES)assert.equal(sceneDirection(scene,"spring park").scene,scene);assert.deepEqual(sceneDirection("custom"," spring park "),{scene:"custom",customScene:"spring park"});assert.equal("preferredEnvironments" in sceneDirection("clean_studio",""),false)});
test("Brand Model mockup requires a durable selected reference",()=>{assert.equal(canCreateBrandModelMockup("brand_model","model-1"),false);assert.equal(canCreateBrandModelMockup("brand_model","model-1","/objects/model.png"),true)});
test("pending paid candidates recover while selected references remain authoritative",()=>{const selected={id:"selected",reference_object_paths:["/objects/one"]};const pending={id:"pending",reference_object_paths:["/objects/one","/objects/two","/objects/three"]};assert.equal(isPendingCandidateSet(selected),false);assert.equal(isPendingCandidateSet(pending),true);assert.equal(findPendingCandidateSet([selected,pending])?.id,"pending")});
